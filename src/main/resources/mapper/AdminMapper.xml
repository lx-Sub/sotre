<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbiter.hrm.mapper.AdminMapper">

    <select id="selectAll" resultType="com.rabbiter.hrm.entity.Admin">
        SELECT * FROM admin WHERE status = 1 ORDER BY create_time DESC
    </select>

    <select id="selectById" resultType="com.rabbiter.hrm.entity.Admin">
        SELECT * FROM admin WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultType="com.rabbiter.hrm.entity.Admin">
        SELECT * FROM admin WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin (username, password, nickname, email, phone, avatar,
                          role, status, create_by, create_time)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{avatar},
                #{role}, #{status}, #{createBy}, #{createTime})
    </insert>

    <update id="updateById">
        UPDATE admin
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="lastLoginIp != null">last_login_ip = #{lastLoginIp},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM admin WHERE id = #{id}
    </delete>

</mapper>